name: Paper Trading Bots

on:
  schedule:
    # Trump bot: cada hora en días hábiles, 13:00–21:00 UTC (9AM-5PM ET)
    - cron: '0 13,14,15,16,17,18,19,20,21 * * 1-5'
    # GameStop + Breakout50d + BB Bounce + Pullback: días hábiles a las 21:30 UTC
    - cron: '30 21 * * 1-5'
    # FVG: días hábiles a las 13:30 UTC (9:30 AM ET = apertura)
    - cron: '30 13 * * 1-5'
    # ICT: días hábiles a las 18:30 UTC (14:30 PM ET)
    - cron: '30 18 * * 1-5'
    # Gap + ORB + Momentum: días hábiles a las 14:30 UTC (10:30 AM ET)
    - cron: '30 14 * * 1-5'
    # Reporte diario: días hábiles a las 22:00 UTC (después del cierre)
    - cron: '0 22 * * 1-5'
    # Momentum SP500: diariamente a las 21:45 UTC (actualiza precios, rebalancea si es primer día del mes)
    - cron: '45 21 * * 1-5'
    # Memecoins: todos los días a las 22:15 UTC (actualiza precios, rebalancea los lunes)
    - cron: '15 22 * * *'
    # Polymarket: todos los días a las 22:30 UTC (chequea señales y exits diariamente)
    - cron: '30 22 * * *'
    # Soccer bots: todos los días a las 23:00–23:20 UTC (después de partidos europeos)
    - cron: '0 23 * * *'
    - cron: '10 23 * * *'
    - cron: '20 23 * * *'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  trump-bot:
    name: Trump Sentiment Bot
    runs-on: ubuntu-latest
    if: (github.event.schedule == '0 13,14,15,16,17,18,19,20,21 * * 1-5' || github.event_name == 'workflow_dispatch')

    steps:
      - uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'
          cache-dependency-path: trump-strategy/requirements.txt

      - name: Instalar dependencias
        run: pip install -r trump-strategy/requirements.txt

      - name: Correr Trump bot
        working-directory: trump-strategy
        run: python bot.py

      - name: Guardar portfolio
        run: |
          git config --local user.email "bot@github-actions"
          git config --local user.name "Paper Trading Bot"
          git add trump-strategy/data/portfolio.json trump-strategy/data/processed_articles.json
          git diff --staged --quiet || git commit -m "bot(trump): update $(date -u +'%Y-%m-%d %H:%M')"
          for i in 1 2 3 4 5; do
            git pull --rebase origin main && git push && break
            echo "Intento $i fallido, reintentando en $((i * 5))s..."
            sleep $((i * 5))
          done

  gamestop-bot:
    name: GameStop Squeeze Bot
    runs-on: ubuntu-latest
    if: github.event.schedule == '30 21 * * 1-5' || github.event_name == 'workflow_dispatch'

    steps:
      - uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'
          cache-dependency-path: gamestop-squeeze/requirements.txt

      - name: Instalar dependencias
        run: pip install -r gamestop-squeeze/requirements.txt

      - name: Scanner mensual (solo el 1ro de cada mes)
        working-directory: gamestop-squeeze
        run: |
          if [ "$(date +%d)" = "01" ]; then
            python run.py --scan
          fi

      - name: Update diario
        working-directory: gamestop-squeeze
        run: python run.py --update

      - name: Guardar portfolio
        run: |
          git config --local user.email "bot@github-actions"
          git config --local user.name "Paper Trading Bot"
          git add gamestop-squeeze/data/portfolio.json
          git diff --staged --quiet || git commit -m "bot(gamestop): update $(date -u +'%Y-%m-%d')"
          for i in 1 2 3 4 5; do
            git pull --rebase origin main && git push && break
            echo "Intento $i fallido, reintentando en $((i * 5))s..."
            sleep $((i * 5))
          done

  fvg-bot:
    name: FVG Strategy Bot
    runs-on: ubuntu-latest
    if: github.event.schedule == '30 13 * * 1-5' || github.event_name == 'workflow_dispatch'

    steps:
      - uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Instalar dependencias
        run: pip install yfinance pandas numpy

      - name: Correr FVG bot
        working-directory: fvg-strategy
        run: python bot.py

      - name: Guardar portfolio
        run: |
          git config --local user.email "bot@github-actions"
          git config --local user.name "Paper Trading Bot"
          git add fvg-strategy/data/portfolio.json
          git diff --staged --quiet || git commit -m "bot(fvg): update $(date -u +'%Y-%m-%d')"
          for i in 1 2 3 4 5; do
            git pull --rebase origin main && git push && break
            echo "Intento $i fallido, reintentando en $((i * 5))s..."
            sleep $((i * 5))
          done

  ict-bot:
    name: ICT Strategy Bot
    runs-on: ubuntu-latest
    if: github.event.schedule == '30 18 * * 1-5' || github.event_name == 'workflow_dispatch'

    steps:
      - uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Instalar dependencias
        run: pip install yfinance pandas numpy

      - name: Correr ICT bot
        working-directory: ict-strategy
        run: python bot.py

      - name: Guardar portfolio
        run: |
          git config --local user.email "bot@github-actions"
          git config --local user.name "Paper Trading Bot"
          git add ict-strategy/data/portfolio.json
          git diff --staged --quiet || git commit -m "bot(ict): update $(date -u +'%Y-%m-%d')"
          for i in 1 2 3 4 5; do
            git pull --rebase origin main && git push && break
            echo "Intento $i fallido, reintentando en $((i * 5))s..."
            sleep $((i * 5))
          done

  gap-bot:
    name: Gap & Go Bot
    runs-on: ubuntu-latest
    if: github.event.schedule == '30 14 * * 1-5' || github.event_name == 'workflow_dispatch'

    steps:
      - uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Instalar dependencias
        run: pip install yfinance pandas numpy

      - name: Correr Gap bot
        working-directory: gap-strategy
        run: python bot.py

      - name: Guardar portfolio
        run: |
          git config --local user.email "bot@github-actions"
          git config --local user.name "Paper Trading Bot"
          git add gap-strategy/data/portfolio.json
          git diff --staged --quiet || git commit -m "bot(gap): update $(date -u +'%Y-%m-%d')"
          for i in 1 2 3 4 5; do
            git pull --rebase origin main && git push && break
            echo "Intento $i fallido, reintentando en $((i * 5))s..."
            sleep $((i * 5))
          done

  orb-breakout-bot:
    name: ORB + Breakout Bot
    runs-on: ubuntu-latest
    if: github.event.schedule == '30 14 * * 1-5' || github.event_name == 'workflow_dispatch'

    steps:
      - uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Instalar dependencias
        run: pip install yfinance pandas numpy

      - name: Correr ORB+Breakout bot
        working-directory: orb-breakout-strategy
        run: python bot.py

      - name: Guardar portfolio
        run: |
          git config --local user.email "bot@github-actions"
          git config --local user.name "Paper Trading Bot"
          git add orb-breakout-strategy/data/portfolio.json
          git diff --staged --quiet || git commit -m "bot(orb-breakout): update $(date -u +'%Y-%m-%d')"
          for i in 1 2 3 4 5; do
            git pull --rebase origin main && git push && break
            echo "Intento $i fallido, reintentando en $((i * 5))s..."
            sleep $((i * 5))
          done

  breakout50d-bot:
    name: Breakout 50d Bot
    runs-on: ubuntu-latest
    if: github.event.schedule == '30 21 * * 1-5' || github.event_name == 'workflow_dispatch'

    steps:
      - uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Instalar dependencias
        run: pip install yfinance pandas numpy

      - name: Correr Breakout50d bot
        working-directory: breakout50d-strategy
        run: python bot.py

      - name: Guardar portfolio
        run: |
          git config --local user.email "bot@github-actions"
          git config --local user.name "Paper Trading Bot"
          git add breakout50d-strategy/data/portfolio.json
          git diff --staged --quiet || git commit -m "bot(breakout50d): update $(date -u +'%Y-%m-%d')"
          for i in 1 2 3 4 5; do
            git pull --rebase origin main && git push && break
            echo "Intento $i fallido, reintentando en $((i * 5))s..."
            sleep $((i * 5))
          done

  momentum-bot:
    name: Momentum 10d Bot
    runs-on: ubuntu-latest
    if: github.event.schedule == '30 14 * * 1-5' || github.event_name == 'workflow_dispatch'

    steps:
      - uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Instalar dependencias
        run: pip install yfinance pandas numpy

      - name: Correr Momentum bot
        working-directory: momentum-strategy
        run: python bot.py

      - name: Guardar portfolio
        run: |
          git config --local user.email "bot@github-actions"
          git config --local user.name "Paper Trading Bot"
          git add momentum-strategy/data/portfolio.json
          git diff --staged --quiet || git commit -m "bot(momentum): update $(date -u +'%Y-%m-%d')"
          for i in 1 2 3 4 5; do
            git pull --rebase origin main && git push && break
            echo "Intento $i fallido, reintentando en $((i * 5))s..."
            sleep $((i * 5))
          done

  bb-bounce-bot:
    name: BB Lower Bounce Bot
    runs-on: ubuntu-latest
    if: github.event.schedule == '30 21 * * 1-5' || github.event_name == 'workflow_dispatch'

    steps:
      - uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Instalar dependencias
        run: pip install yfinance pandas numpy

      - name: Correr BB Bounce bot
        working-directory: bb-bounce-strategy
        run: python bot.py

      - name: Guardar portfolio
        run: |
          git config --local user.email "bot@github-actions"
          git config --local user.name "Paper Trading Bot"
          git add bb-bounce-strategy/data/portfolio.json
          git diff --staged --quiet || git commit -m "bot(bb-bounce): update $(date -u +'%Y-%m-%d')"
          for i in 1 2 3 4 5; do
            git pull --rebase origin main && git push && break
            echo "Intento $i fallido, reintentando en $((i * 5))s..."
            sleep $((i * 5))
          done

  pullback-bot:
    name: Pullback EMA50 + ADX Bot
    runs-on: ubuntu-latest
    if: github.event.schedule == '30 21 * * 1-5' || github.event_name == 'workflow_dispatch'

    steps:
      - uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Instalar dependencias
        run: pip install yfinance pandas numpy

      - name: Correr Pullback bot
        working-directory: pullback-strategy
        run: python bot.py

      - name: Guardar portfolio
        run: |
          git config --local user.email "bot@github-actions"
          git config --local user.name "Paper Trading Bot"
          git add pullback-strategy/data/portfolio.json
          git diff --staged --quiet || git commit -m "bot(pullback): update $(date -u +'%Y-%m-%d')"
          for i in 1 2 3 4 5; do
            git pull --rebase origin main && git push && break
            echo "Intento $i fallido, reintentando en $((i * 5))s..."
            sleep $((i * 5))
          done

  momentum-sp500-bot:
    name: Momentum SP500 Bot
    runs-on: ubuntu-latest
    if: github.event.schedule == '45 21 * * 1-5' || github.event_name == 'workflow_dispatch'

    steps:
      - uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Instalar dependencias
        run: pip install yfinance pandas numpy requests

      - name: Correr Momentum SP500 bot
        working-directory: momentum-sp500-strategy
        run: python bot.py

      - name: Guardar portfolio
        run: |
          git config --local user.email "bot@github-actions"
          git config --local user.name "Paper Trading Bot"
          git add momentum-sp500-strategy/data/portfolio.json
          git diff --staged --quiet || git commit -m "bot(momentum-sp500): update $(date -u +'%Y-%m-%d')"
          for i in 1 2 3 4 5; do
            git pull --rebase origin main && git push && break
            echo "Intento $i fallido, reintentando en $((i * 5))s..."
            sleep $((i * 5))
          done

  memecoin-bot:
    name: Memecoin Momentum Bot
    runs-on: ubuntu-latest
    if: github.event.schedule == '15 22 * * *' || github.event_name == 'workflow_dispatch'

    steps:
      - uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Instalar dependencias
        run: pip install requests

      - name: Correr Memecoin bot
        working-directory: memecoin-strategy
        run: python bot.py

      - name: Guardar portfolio
        run: |
          git config --local user.email "bot@github-actions"
          git config --local user.name "Paper Trading Bot"
          git add memecoin-strategy/data/portfolio.json
          git diff --staged --quiet || git commit -m "bot(memecoin): update $(date -u +'%Y-%m-%d')"
          for i in 1 2 3 4 5; do
            git pull --rebase origin main && git push && break
            echo "Intento $i fallido, reintentando en $((i * 5))s..."
            sleep $((i * 5))
          done

  polymarket-bot:
    name: Polymarket Momentum Bot
    runs-on: ubuntu-latest
    if: github.event.schedule == '30 22 * * *' || github.event_name == 'workflow_dispatch'

    steps:
      - uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Instalar dependencias
        run: pip install requests

      - name: Correr Polymarket bot
        working-directory: polymarket-strategy
        run: python bot.py

      - name: Guardar portfolio
        run: |
          git config --local user.email "bot@github-actions"
          git config --local user.name "Paper Trading Bot"
          git add polymarket-strategy/data/portfolio.json
          git diff --staged --quiet || git commit -m "bot(polymarket): update $(date -u +'%Y-%m-%d')"
          for i in 1 2 3 4 5; do
            git pull --rebase origin main && git push && break
            echo "Intento $i fallido, reintentando en $((i * 5))s..."
            sleep $((i * 5))
          done

  polysoccer-favorite-bot:
    name: Soccer Favorite Bot
    runs-on: ubuntu-latest
    if: github.event.schedule == '0 23 * * *' || github.event_name == 'workflow_dispatch'

    steps:
      - uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Instalar dependencias
        run: pip install requests

      - name: Correr Soccer Favorite bot
        working-directory: polysoccer-favorite
        run: python bot.py

      - name: Guardar portfolio
        run: |
          git config --local user.email "bot@github-actions"
          git config --local user.name "Paper Trading Bot"
          git add polysoccer-favorite/data/portfolio.json
          git diff --staged --quiet || git commit -m "bot(soccer-favorite): update $(date -u +'%Y-%m-%d')"
          for i in 1 2 3 4 5; do
            git pull --rebase origin main && git push && break
            echo "Intento $i fallido, reintentando en $((i * 5))s..."
            sleep $((i * 5))
          done

  polysoccer-arb-bot:
    name: Soccer Arb Bot
    runs-on: ubuntu-latest
    if: github.event.schedule == '10 23 * * *' || github.event_name == 'workflow_dispatch'

    steps:
      - uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Instalar dependencias
        run: pip install requests

      - name: Correr Soccer Arb bot
        working-directory: polysoccer-arb
        env:
          ODDS_API_KEY: ${{ secrets.ODDS_API_KEY }}
        run: python bot.py

      - name: Guardar portfolio
        run: |
          git config --local user.email "bot@github-actions"
          git config --local user.name "Paper Trading Bot"
          git add polysoccer-arb/data/portfolio.json
          git diff --staged --quiet || git commit -m "bot(soccer-arb): update $(date -u +'%Y-%m-%d')"
          for i in 1 2 3 4 5; do
            git pull --rebase origin main && git push && break
            echo "Intento $i fallido, reintentando en $((i * 5))s..."
            sleep $((i * 5))
          done

  polysoccer-model-bot:
    name: Soccer Elo Model Bot
    runs-on: ubuntu-latest
    if: github.event.schedule == '20 23 * * *' || github.event_name == 'workflow_dispatch'

    steps:
      - uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Instalar dependencias
        run: pip install requests

      - name: Correr Soccer Elo Model bot
        working-directory: polysoccer-model
        run: python bot.py

      - name: Guardar portfolio
        run: |
          git config --local user.email "bot@github-actions"
          git config --local user.name "Paper Trading Bot"
          git add polysoccer-model/data/portfolio.json
          git diff --staged --quiet || git commit -m "bot(soccer-model): update $(date -u +'%Y-%m-%d')"
          for i in 1 2 3 4 5; do
            git pull --rebase origin main && git push && break
            echo "Intento $i fallido, reintentando en $((i * 5))s..."
            sleep $((i * 5))
          done

  daily-report:
    name: Reporte Diario
    runs-on: ubuntu-latest
    if: github.event.schedule == '0 22 * * 1-5' || github.event_name == 'workflow_dispatch'

    steps:
      - uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          fetch-depth: 0

      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Instalar dependencias
        run: pip install yfinance pandas numpy

      - name: Generar reporte
        run: python report.py

      - name: Guardar reporte
        run: |
          git config --local user.email "bot@github-actions"
          git config --local user.name "Paper Trading Bot"
          git add reports/
          git diff --staged --quiet || git commit -m "report: daily $(date -u +'%Y-%m-%d')"
          for i in 1 2 3 4 5; do
            git pull --rebase origin main && git push && break
            echo "Intento $i fallido, reintentando en $((i * 5))s..."
            sleep $((i * 5))
          done
